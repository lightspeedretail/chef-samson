<% %w(
	buddy_check
	docker
	jenkins
	github
	google
	request_access
	slack
).each do |key| %>
<% if defined?("@#{key}") and variables = send("@#{key}") %>
<%= render "env/#{key}.erb", variables: variables %>
<% end %>
<% end %>
<%= render "env/configs.erb", variables: @configs %>
