<% %w(
	buddy_check
	docker
	jenkins
	github
	google
	request_access
	slack
).each do |key| %>
<% if variables = instance_variable_get("@#{key}") %>
<%= render "env/#{key}.erb", variables: variables %>
<% end %>
<% end %>
<%= render "env/configs.erb", variables: @configs %>
