DOCKER_FEATURE=<%= @registry ? 1 : 0 %>
DOCKER_REGISTRY<%= @registry %>
DOCKER_URL=<%= @url %>
DOCKER_KEEP_BUILT_IMGS=<%= @keep_built_imags ? 1 : 0 %>
DOCKER_REPO_NAMESPACE=<%= @repo_namespace || 'samson' %>
